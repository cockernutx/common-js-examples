import { Alert } from 'react-bootstrap';
import styled from 'styled-components';

export const RandomNumberDisplay = styled(Alert)`
margin-top: 10px;
text-align: center;
font-size: 32px;
`;

export const ResultDisplay = styled(Alert)`
margin-top: 10px;
text-align: center;
font-size: 32px;

`;